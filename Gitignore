
### .gitignore

```gitignore
# Ignorar arquivos de log
*.log

# Ignorar arquivos temporários
*.tmp

# Ignorar arquivos de banco de dados
*.sql

# Ignorar arquivos de backup
*.bak

# Ignorar arquivos de configuração local
config/

# Ignorar arquivos de IDEs
.idea/
.vscode/
.DS_Store

Enunciado: Sistema de Gerenciamento de Inventário
Objetivo
Desenvolver um sistema de gerenciamento de inventário para um armazém que permita o controle de categorias de produtos, fornecedores e produtos em estoque. O sistema deve permitir a manipulação e consulta dos dados relacionados a esses componentes.

Requisitos
Criação do Banco de Dados:

O banco de dados deve ser criado com o nome InventarioArmazem.
Tabelas:

Categorias: Deve armazenar informações das categorias de produtos com os campos CategoriaID e Nome.
Fornecedores: Deve armazenar informações dos fornecedores com os campos FornecedorID, Nome, Contato, e Endereco.
Produtos: Deve armazenar informações dos produtos com os campos ProdutoID, Descricao, Preco, QuantidadeEmEstoque, e CategoriaID. Deve haver um relacionamento de chave estrangeira com a tabela Categorias.
Operações:

Inserir Dados: Adicione dados de exemplo para categorias, fornecedores e produtos.
Consultar Dados: Realize consultas para listar todos os produtos, filtrar produtos por categoria, e encontrar fornecedores que fornecem produtos acima de um determinado preço.
Atualizar Dados: Permita a atualização de preços de produtos, a modificação de descrições de categorias, e a alteração de endereços de fornecedores.
Excluir Dados: Permita a exclusão de produtos, categorias e fornecedores, assegurando que as referências e integridade dos dados sejam mantidas.
Consultas SQL:

Deve ser capaz de gerar relatórios e consultas para mostrar todos os produtos disponíveis, produtos por categoria e fornecedores que fornecem produtos acima de um certo preço.
Entregáveis
Script SQL completo para criação e manipulação do banco de dados.
Instruções claras para instalação e uso do banco de dados.
Documentação para consultas exemplares e operações básicas.
Avaliação
O projeto será avaliado com base na clareza e organização do banco de dados, na eficácia das consultas SQL e na adequação das operações implementadas ao cenário proposto.

